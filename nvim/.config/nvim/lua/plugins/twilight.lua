-- =========================================================
-- Plugin: Twilight (Rob Meijerink)
-- =========================================================
return {
    "folke/twilight.nvim",
    cmd = "Twilight",
    opts = {
        -- Keep it clean and focused
        dimming = { alpha = 0.25 },
        context = 10,
    }
}
